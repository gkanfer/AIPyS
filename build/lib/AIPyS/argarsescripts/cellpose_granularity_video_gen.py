# Kernel Size and Granularity Analysis
# python cellpose_granularity_video_gen --dir <images_directory> --pretrained_model cyto --diameter <your_diameter_value> --save_tif
import argparse
import sys
import h5py
from pathlib import Path
import os

user_parameters_path = os.path.join(Path.home(), '.AIPyS', 'parameters.h5')

def granularity_celppose_video_gen():
    pass
